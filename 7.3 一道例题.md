<pre>
输入图片（原材料）
      │
      ▼
Conv1（6 个质检员）
      │
      │ 每个质检员负责找一种简单特征：
      │ 边缘、横线、竖线、角点……
      ▼
得到 6 张 Feature Maps
      │
      ▼
MaxPool（压缩信息）
      │
      ▼
Conv2（12 个高级质检员）
      │
      │ 把前面的简单特征组合起来，
      │ 找眼睛、耳朵、圆圈等更复杂的结构
      ▼
得到 12 张更高级的 Feature Maps
      │
      ▼
MaxPool（再次压缩）
      │
      ▼
Flatten（把所有信息整理成一个长向量）
      │
      ▼
Linear（最终决策）
      │
      ▼
输出 10 个数字的概率
      │
      ▼
预测结果：0 ~ 9
</pre>
